services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - SECRET_KEY=change-me-in-prod
      - DATABASE_URL=sqlite:////app/data/app.db
